# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node (if any scripts need it)
node_modules/

# Claude Code local config
.claude/

# Output
output/
*.log

# Temporary upstream clones (used by sync-android-skills workflow)
_upstream/

# Internal docs — commercial strategy, partnership, designs (NEVER commit)
docs/designs/
docs/plans/
docs/strategy/
docs/roadmap-next-steps.md

# Git worktrees (project-local)
.worktrees/

# GoReleaser build artifacts
cli/dist/

# Embedded catalog (populated by go generate before build)
cli/internal/embedded/plugins/

# Brain runtime state (creado por `mobiai brain init` al correr el CLI en el repo)
.mobiai/

# Go test binaries (created when using `go test -c -o test.exe` workaround)
**/test.exe
**/*.test.exe
